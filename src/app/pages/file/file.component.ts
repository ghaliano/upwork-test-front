import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'batiste-file',
  template: `
    <router-outlet></router-outlet>
  `,
})
export class FileComponent implements OnInit{

  ngOnInit(): void {
  }
}